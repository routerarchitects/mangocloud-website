---
slug: /operations/device-onboarding/gatewayjson-configuration
title: gateway.json Configuration
---

The device uses `gateway.json` to discover the controller address.

```bash
vi /etc/ucentral/gateway.json
```

Update controller address and port:

- Server/URL: `<DOMAIN_NAME>`
- Port: `15002`
